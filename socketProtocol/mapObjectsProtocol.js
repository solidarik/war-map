const logHelper = require('../helper/logHelper');
const ServerProtocol = require('../libs/serverProtocol')

class MapObjectsProtocol extends ServerProtocol {

    init() {
    }
}

module.exports = new MapObjectsProtocol();